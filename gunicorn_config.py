bind = "0.0.0.0:5000"  # Address and port to bind to
workers = 2  # Number of worker processes
timeout = 30  # Worker timeout in seconds
loglevel = "info"  # Log level
accesslog = "-"  # Access log output, '-' means stdout
errorlog = "-"  # Error log output, '-' means stderr
